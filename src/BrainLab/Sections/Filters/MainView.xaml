<UserControl x:Class="BrainLab.Sections.Filters.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid  Margin="4">
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Button Name="FilterSubjects" Content="Apply Filters" />
		
		<Label Content="Groups" Grid.Row="1" Grid.Column="0" Foreground="{DynamicResource MainWindowText}" Padding="5,2"/>
		<ItemsControl Name="Groups" Grid.Row="2" Grid.Column="0" />

		<Label Content="Data Types" Grid.Row="1" Grid.Column="1"  Foreground="{DynamicResource MainWindowText}" Padding="5,2"/>
		<ItemsControl Name="DataTypes" Grid.Row="2" Grid.Column="1" />
	</Grid>
</UserControl>
