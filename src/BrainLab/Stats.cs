using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace BrainLab.Studio
{
	public class Stats
	{
		public static double TStat(IEnumerable<double> g1, IEnumerable<double> g2)
		{
			//var m1 = Statistics.Mean(g1);
			//var m2 = Statistics.Mean(g2);
			//var d1 = Statistics.StandardDeviation(g1);
			//var d2 = Statistics.StandardDeviation(g2);
			//var v1 = Statistics.Variance(g1);
			//var v2 = Statistics.Variance(g2);

			//return (m1 - m2) / Math.Sqrt((Math.Pow(d1, 2) / 29) + (Math.Pow(d2, 2) / 29));

			return 0;
		}

		public static double MeanDiff(IEnumerable<double> g1, IEnumerable<double> g2)
		{
			//var m1 = Statistics.Mean(g1);
			//var m2 = Statistics.Mean(g2);

			//return m1 - m2;

			return 0;
		}
	}
}
